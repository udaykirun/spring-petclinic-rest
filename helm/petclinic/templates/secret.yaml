apiVersion: v1
kind: Secret
metadata:
  name: petclinic-db-secret
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  POSTGRES_USER: {{ .Values.postgresql.user }}
  POSTGRES_PASS: {{ .Values.postgresql.password }}
